{
  "name": "ozon-mcp-server",
  "version": "0.0.1",
  "description": "MCP server for searching Ozon products and reading product details and reviews. A single long-lived headless Chromium passes Ozon's anti-bot challenge; data is fetched as JSON from Ozon's internal composer API.",
  "type": "module",
  "bin": {
    "ozon-mcp-server": "src/index.js"
  },
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test:parse": "node test/parse.test.js"
  },
  "keywords": ["mcp", "ozon", "marketplace", "search", "playwright"],
  "license": "MIT",
  "engines": { "node": ">=20" },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.6.1",
    "playwright": "1.60.0",
    "zod": "^3.25.0"
  }
}
